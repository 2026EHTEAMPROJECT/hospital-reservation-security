apiVersion: apps/v1
kind: Deployment
metadata:
  name: login-service
  namespace: hospital-dev
  labels:
    app: login-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: login-service
  template:
    metadata:
      labels:
        app: login-service
    spec:
      containers:
        - name: login-service
          # CI가 빌드/푸시할 이미지. kustomization.yaml의 images.newTag(커밋 SHA)가 태그를 덮어쓴다.
          image: edenriel/hospital-login:latest
          # SHA 태그는 불변이므로 IfNotPresent로 충분(불필요한 재pull 방지).
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
          env:
            - name: KEYCLOAK_TOKEN_URI
              value: http://keycloak.hospital-dev.svc.cluster.local:8080/realms/hospital/protocol/openid-connect/token
            - name: KEYCLOAK_CLIENT_ID
              value: hospital-frontend
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 20
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 40
            periodSeconds: 30
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            runAsUser: 1001
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: login-service
  namespace: hospital-dev
  labels:
    app: login-service
spec:
  type: ClusterIP
  selector:
    app: login-service
  ports:
    - name: http
      protocol: TCP
      port: 8085
      targetPort: 8085
